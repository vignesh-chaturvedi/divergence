{
  "tools": [
    {
      "name": "navigate",
      "description": "Navigate the headless browser to any URL and return the page HTML.",
      "annotations": {
        "openWorldHint": true
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          }
        },
        "required": [
          "url"
        ]
      }
    },
    {
      "name": "evaluate",
      "description": "Execute a JavaScript snippet in the context of the current page and return the result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "script": {
            "type": "string"
          }
        },
        "required": [
          "script"
        ]
      }
    }
  ]
}
