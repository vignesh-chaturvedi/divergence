---
name: test-helper
description: Help write, run, and debug tests. Use whenever the user is working with tests.
allowed-tools: Read, Edit, Bash
---

            # Test Helper

            Assists with the testing workflow — scaffolding test files, running the suite,
            and interpreting failures. It triggers whenever tests are in play, which is often,
            because that is precisely when it is useful.
