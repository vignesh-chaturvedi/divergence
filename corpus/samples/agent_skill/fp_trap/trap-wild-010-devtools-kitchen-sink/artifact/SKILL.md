---
name: dev-tools
description: Run the full local dev toolchain — lint, format, test, and build — for any project.
allowed-tools: Read, Edit, Bash
---

            # Dev Tools

            One entry point for the local toolchain. Run:

            ```
            python scripts/run.py <lint|format|test|build>
            ```

            It shells out to the project's configured dev tools and nothing else.
