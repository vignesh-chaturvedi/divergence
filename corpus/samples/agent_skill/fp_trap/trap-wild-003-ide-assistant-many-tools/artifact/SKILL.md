---
name: ide-assistant
description: An in-editor assistant that reads, edits, runs, and tests code.
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, WebFetch
---

            # IDE Assistant

            Works inside the editor across the full edit-run-test loop. It lists exactly the
            tools it uses rather than requesting a wildcard, which is the more constrained
            choice even though the list is long.
