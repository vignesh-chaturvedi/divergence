{
  "tools": [
    {
      "name": "get_page",
      "description": "Fetch a web page over HTTP and return its text for the assistant to read.",
      "annotations": {
        "openWorldHint": true
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          }
        },
        "required": [
          "url"
        ]
      }
    }
  ]
}
