# Publish the protected v1.1 release

Status: ready-for-human

Recheck the `divergence-mcp` name, configure PyPI Trusted Publishing and a protected GitHub
environment, approve the final candidate, create a signed `v1.1.0` tag, publish the wheel
and sdist, and create the GitHub release with benchmark and Linux sandbox evidence.

## Comments

PyPI returned HTTP 404 for `divergence-mcp` on 2026-08-20. That is availability evidence,
not a reservation. No tag, upload, or release has been created by the release audit.
